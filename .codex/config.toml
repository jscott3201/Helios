# Gemma4D Codex project settings.
# Merge with user-level Codex config as needed.

[agents]
max_threads = 8
max_depth = 1
job_max_runtime_seconds = 1800

# The custom agent files in .codex/agents/ request xhigh reasoning and inherit
# the active/parent Codex model. Launch Codex with your available GPT-5.5 Codex
# model or set the model in user-level config/profile if desired.
