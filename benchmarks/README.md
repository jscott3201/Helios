# Benchmarks

Benchmark prompt sets and raw outputs live here.

- `prompts/` stores reusable prompt fixtures.
- `out/` stores generated benchmark/profiling output and is ignored except for
  directory placeholders.
