# M02 Evidence Ledger

This file records the commands used to verify M02 Gemma 4 tokenizer, chat, and config work.

## Commands

Run on 2026-06-30 from `/Users/justin/Development/Helios`.

| Command | Result | Evidence |
|---|---|---|
| `cargo test -p gemma4d-tokenizer` | Passed | 4 tests covered supported Gemma 4 config validation, unsupported config failure text, fixture tokenizer loading, stop-token IDs, detokenization boundary round trip, and tokenizer file hash stability. |
| `cargo test -p gemma4d-chat` | Passed | 4 tests covered system role rendering, thinking-mode prompt/hash changes, fixture loading, and exact token ID equality for `simple_chat`, `system_prompt`, `code_rust`, `code_python`, `long_prefix_4k`, and `tool_call_shape`. |
| `cargo test -p gemma4d-cache` | Passed | 2 tests covered deterministic cache-key input hashing and prompt token-prefix order sensitivity. |
| `cargo fmt --all --check` | Passed | No formatting diff reported. |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | Passed | Workspace checked with warnings denied. |
| `make verify` | Passed | Ran format, clippy, workspace tests, focused `cargo test -p gemma4d-ffi`, and native smoke build. |
| `rg -n "tokenizer.json\\|Gemma4Unified\\|expected_token\\|gemma-4\\|gemma4" tests _spec references spec milestones crates -g '!target/**'` | Passed | Confirmed `_spec` does not contain a real tokenizer snapshot; M02 fixtures are local checked-in reference fixtures. |

## Fixture Reference Notes

- Reference source for token ID equality is `tests/fixtures/tokenizer/tokenizer.json` plus checked-in prompt fixture token IDs under `tests/fixtures/prompts/`.
- `long_prefix_4k.json` stores a compact repeated reference expression; tests expand it before comparing exact token IDs.
- The fixture tokenizer is intentionally labelled `gemma4d_fixture_tokenizer_v1`; it is not claimed to be a downloaded Hugging Face/Gemma tokenizer.
- No full 12B model artifact was downloaded or loaded.
- No benchmark or profiling run was performed, so no raw benchmark output was added under `benchmarks/out/M02/`.
