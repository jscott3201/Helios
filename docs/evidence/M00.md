# M00 Evidence Ledger

This file records the commands used to verify M00 repository bootstrap.

## Commands

Run on 2026-06-30 from `/Users/justin/Development/Helios`.

| Command | Result | Evidence |
|---|---|---|
| `cargo metadata --format-version=1 --no-deps` | Passed | Cargo listed all 13 workspace crates and target directory `target`. |
| `cargo fmt --all --check` | Passed | No formatting diff reported. |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | Passed | All placeholder crates checked with warnings denied. |
| `cargo test --workspace --all-targets --all-features` | Passed | 13 placeholder library tests passed; binary targets had 0 tests. |
| `cmake -S native/gemma4_mlx -B target/native-smoke` | Passed | Configured with AppleClang 17 and documented that MLX is optional for the M00 smoke build. |
| `make verify` | Passed | Ran metadata, fmt, clippy, tests, CMake configure, and native smoke build. |

## Environment Notes

- `cmake` was initially unavailable in PATH and was installed with `brew install cmake`.
- The native smoke build produced `target/native-smoke/libgemma4_mlx_smoke.a`.
- No full model download was performed.
- No benchmark or profiling run was performed, so no raw benchmark output was added under `benchmarks/out/M00/`.
