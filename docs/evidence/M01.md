# M01 Evidence Ledger

This file records the commands used to verify M01 native MLX loader and FFI smoke.

## Commands

Run on 2026-06-30 from `/Users/justin/Development/Helios`.

| Command | Result | Evidence |
|---|---|---|
| `cargo test -p gemma4d-ffi` | Passed | Built `gemma4_mlx` through `crates/gemma4d-ffi/build.rs`; ran 5 FFI tests covering version query, target/KV lifecycle, invalid config error, raw null-pointer errors, and M01 unsupported execution stubs. |
| `find target/debug/build -path '*gemma4_mlx-build/libgemma4_mlx.a' -print` | Passed | Confirmed Cargo build script produced native static libraries under `target/debug/build/gemma4d-ffi-*/out/gemma4_mlx-build/libgemma4_mlx.a`. |
| `cargo fmt --all --check` | Passed | No formatting diff reported. |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | Passed | Workspace, including `gemma4d-ffi`, checked with warnings denied. |
| `cargo test --workspace --all-targets --all-features` | Passed | Workspace tests passed, including 5 `gemma4d-ffi` smoke tests. |
| `./scripts/native-smoke.sh` | Passed | CMake configured and built `target/native-smoke/libgemma4_mlx.a`. |
| `./scripts/mlx-diagnostics.sh` | Expected diagnostic failure | CMake and AppleClang were found, but `find_package(MLX CONFIG REQUIRED)` could not locate `MLXConfig.cmake`/`mlx-config.cmake`; this is optional and not required for M01 smoke tests. |
| `make verify` | Passed | Ran format, clippy, workspace tests, focused `cargo test -p gemma4d-ffi`, and native smoke build. |

## Environment Notes

- `cmake` was available at `/opt/homebrew/bin/cmake`, version 4.3.4.
- `c++` was available at `/usr/bin/c++`, Apple clang 17.0.0.
- No model artifact was downloaded or loaded.
- No benchmark or profiling run was performed, so no raw benchmark output was added under `benchmarks/out/M01/`.
- MLX is not configured as a CMake package on this machine; the M01 default native build intentionally does not require it.
