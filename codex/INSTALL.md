# Codex Install / Use Notes

This package is laid out as a repository root. Start Codex from this directory or copy these files into the implementation repository root.

Codex should discover:

```text
AGENTS.md
.codex/config.toml
.codex/agents/*.toml
.agents/skills/*/SKILL.md
```

Recommended verification after copying:

```bash
ls AGENTS.md .codex/config.toml .agents/skills
```

Use `codex/goals/*.goal.md` to start durable milestone-focused work.
