# Codex Goal — M05 — Ratatui Operator Console

```text
/goal Complete M05 — Ratatui Operator Console for gemma4d, verified by the acceptance criteria in milestones/M05-tui-operator-console.md, while preserving all hard constraints in AGENTS.md. Implement only the TUI skeleton, offline/mock/file providers, config/benchmark/log/dashboard/help surfaces, tests, snapshots, and terminal lifecycle handling needed by M05. Do not wire live MLX scheduler calls, live chat, MTP, KV mutation, or adapter mutation beyond explicit placeholders. Use spec/13-tui-operator-ux.md and repo-scoped skills. Between iterations, record what changed, which command/test/benchmark was run, what evidence it produced, and the next best action. If blocked, stop with attempted paths, evidence gathered, blocker, and next input needed.
```
