# Codex Goal — Main Project

Use this only after the repository has been created and the user wants Codex to work across multiple milestones.

```text
/goal Implement gemma4d as a Gemma 4 12B-focused Rust 1.95.0 + MLX inference runtime for macOS Apple Silicon, verified milestone-by-milestone by the acceptance criteria in milestones/, while preserving the hard constraints in AGENTS.md: text-only first, narrow MLX C/C++ FFI, 16GB MacBook profile, MTP exactness, adapter-aware cache correctness, Ratatui TUI-driven operator workflows, and no DiffusionGemma or broad general-engine scope. Use each milestone's tests, benchmark reports, and generated artifacts as completion evidence. Between iterations, complete the next unaccepted milestone in order, record evidence, and stop if a blocker requires user input or a milestone cannot be defensibly completed under the current constraints.
```
